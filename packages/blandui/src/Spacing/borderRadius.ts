import convert from '../utils/convert';
import units from './units';

export default {
	null: convert(units.null),
	xs:   convert(units.xs),
	sm:   convert(units.sm),
	md:   convert(units.md),
	lg:   convert(units.lg),
	xl:   convert(units.xl),
	rd:   convert(units.rd),
};
